<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('./head/head');  %> 
    </head>
    <body id="body-index">
    <%- include('./body/navigation');  %>
    <section class="bg-color-3">
        <form method="get" action="patients.new">
            <button class="btn btn-primary mt-3" type="submit">Crear nuevo paciente</button>
        </form>
        <% patients.forEach( (element) => { %>
            <div class="bubble-nurses">
                <a href="patients/<%=element.id%>">Ver más</a>
                <p> <%=  element.name %> </p>
                <p> <%= element.age %> </p>
            </div> 
        <% }) %> 
    </section>
        
    <%- include('./body/javascript');  %>
    </body>
</html>